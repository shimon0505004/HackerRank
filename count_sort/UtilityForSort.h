#pragma once
#include<vector>

namespace UtilityForSort 
{
	std::vector<int> count_sort(std::vector<int> inputArray);
	int max_element_index(const std::vector<int>& inputArray);
	int min_element_index(const std::vector<int>& inputArray);
};